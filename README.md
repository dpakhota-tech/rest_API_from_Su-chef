# rest_API_from_Su-chef API для кафе — от су-шефа 🔥

> **Су-шеф с 2-летним стажем → бэкенд-разработчик.**  
> Готовлю **код так же вкусно, как стейк.**

---

## Что умеет API
| Endpoint | Описание |
|---------|--------|
| `GET /` | Приветствие |
| `GET /меню` | Список блюд |
| `GET /блюдо/{id}` | Блюдо по ID |
| `POST /заказ` | Создать заказ |
| `GET /заказы` | Все заказы |

---

## Как запустить
```bash
pip install fastapi uvicorn
fastapi dev main.py
